server:
  ip: "0.0.0.0"
  port: 8000

database:
  database_url: "sqlite:./data/app.db"
  create_if_missing: true

session:
  cookie:
    name: "session"
    secure: true
    http_only: true
    same_site: "Lax"
  state:
    ttl: "168h"